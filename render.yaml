services:
  - type: web
    name: lumina-emotion-engine
    runtime: python
    pythonVersion: "3.11.0"
    buildCommand: pip install -r requirements-render.txt && python -m app.pipeline.train
    startCommand: uvicorn app.main:app --host 0.0.0.0 --port $PORT